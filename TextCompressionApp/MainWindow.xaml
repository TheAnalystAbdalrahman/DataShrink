<Window x:Class="TextCompressionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Compression App" Height="600" Width="800">
    <Grid>
        <!-- Top Menu -->
        <Menu VerticalAlignment="Top">
            <MenuItem Header="File">
                <MenuItem Header="Open" Click="OpenFile_Click"/>
                <MenuItem Header="Save Results" Click="SaveResults_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>
        </Menu>

        <!-- Tab Control for Algorithms -->
        <TabControl Margin="10,40,10,50">
            <!-- Huffman Coding Tab -->
            <TabItem Header="Huffman Coding">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBox x:Name="HuffmanInputText" Grid.Row="0" Height="100" Margin="10" IsReadOnly="True" TextWrapping="Wrap"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                        <Button Content="Compress" Click="HuffmanCompress_Click" Margin="5" Width="100"/>
                        <Button Content="Decompress" Click="HuffmanDecompress_Click" Margin="5" Width="100"/>
                    </StackPanel>
                    <DataGrid x:Name="HuffmanResultsGrid" Grid.Row="2" Margin="10" AutoGenerateColumns="True"/>
                </Grid>
            </TabItem>

            <!-- Run-Length Coding Tab -->
            <TabItem Header="Run-Length Coding">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBox x:Name="RunLengthInputText" Grid.Row="0" Height="100" Margin="10" IsReadOnly="True" TextWrapping="Wrap"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                        <Button Content="Compress" Click="RunLengthCompress_Click" Margin="5" Width="100"/>
                        <Button Content="Decompress" Click="RunLengthDecompress_Click" Margin="5" Width="100"/>
                    </StackPanel>
                    <DataGrid x:Name="RunLengthResultsGrid" Grid.Row="2" Margin="10" AutoGenerateColumns="True"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
